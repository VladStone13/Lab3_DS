apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: training-service
  template:
    metadata:
      labels:
        app: training-service
    spec:
      containers:
      - image: training-service:snapshot
        name: training-service
    env:
      - name: POSTGRES_ENDPOINT
        value: "jdbc:postgresql://172.17.0.2:5432/"
      - name: POSTGRES_PASSWORD
        value: "13102001"
      - name: POSTGRES_USERNAME
        value: "postgres"